services:
  fnos-gateway:
    build: .
    container_name: fnos-gateway
    ports:
      - "8081:8080"
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
      # 占位示例：替换为你自己生成的强随机值（如 openssl rand -hex 32）
      - SAG_MCP_TOKEN=k7m9x2p_REPLACE_ME
